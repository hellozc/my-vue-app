[build]
builder = "DOCKERFILE"
# 构建上下文为仓库根目录时，Dockerfile 路径相对根目录
dockerfilePath = "layout-consumer/Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 180

[build.buildArgs]
VITE_API_BASE_URL = "${{VITE_API_BASE_URL}}"
VITE_LAYOUT_CODE = "${{VITE_LAYOUT_CODE}}"
