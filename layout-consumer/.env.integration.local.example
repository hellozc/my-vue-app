# 复制为 .env.integration.local 可覆盖联调配置（git 忽略 local 文件时请自行加入 .gitignore）
#
# 小程序 / App 联调无法走 H5 代理，需填写可访问的完整 API 地址，例如：
# VITE_API_BASE_URL=http://192.168.1.100:8080/api
#
# 后端端口与 server/.env 中 PORT 保持一致：
# VITE_API_TARGET=http://localhost:8080
