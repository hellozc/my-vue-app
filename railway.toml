[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["Dockerfile", "deploy/**", "src/**", "package.json"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120

[build.buildArgs]
VITE_API_BASE_URL = "${{VITE_API_BASE_URL}}"
